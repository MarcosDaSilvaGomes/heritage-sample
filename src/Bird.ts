import { Animal } from "./Animal"

class Bird extends Animal {
public canFly: boolean


constructor (name: string ,timeLife : number,wieght:number,peso:number,) {
 super (name, timeLife, , peso, );
 this. = ;

}
}
 export {Bird};